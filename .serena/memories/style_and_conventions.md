Use standard Dart formatting with two-space indentation. Files use snake_case.dart, classes use PascalCase, and members use camelCase. Keep storage logic in services, orchestration in repositories, and UI state in view models. Keep Flutter widget files to one Widget subclass per file; a StatefulWidget may keep its matching private State<T> class in the same file, but helper widgets should be extracted. Prefer clear async APIs returning domain objects. Keep comments brief and only where useful.