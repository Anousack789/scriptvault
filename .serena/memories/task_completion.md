Before handing off code changes, run fvm dart format lib test for touched Dart files or the relevant directories, then fvm flutter analyze and fvm flutter test when feasible. For UI changes, include screenshots or a short recording in PRs when appropriate. Be especially careful with ScriptRunService, macOS entitlements, and storage paths because script execution is intentionally powerful.